# Add Mentorships CSV

Go executable to add new mentorship connections between existing users via
csv file.

Usage:
$ go run add_mentorships_csv.go -file=mentorships.csv [-server=http://hostname]

## Dependencies

go get golang.org/x/crypto/ssh/terminal
